require "spec_helper"

RSpec.describe Landing::Careers::Challenge do
  it "has a version number" do
    expect(Landing::Careers::Challenge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
